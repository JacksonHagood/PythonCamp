# random in python

import random
# random module is built into python
# a module must be imported prior to using it

while True:
    print(random.randint(1, 100))
    # randint() in random will return a random number within a specified range
    # quite easy to get a forever loop
